[workspace]
resolver = "2"
members = ["host", "methods"]

[workspace.package]
version = "0.1.0"
edition = "2021"

# Release profile tuned for faster zkVM proving of the host.
[profile.release]
opt-level = 3
