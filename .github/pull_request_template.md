## Summary

What was fixed or added, and why.

## Changes

What changed, file by file if not obvious from the diff.

## Issues

Resolves #

## Verification

What you ran locally (e.g. `cargo fmt`, `cargo clippy`, `cargo test` from the
relevant Cargo root — see "Cargo roots" in `README.md`) and the results.
State plainly if something could not be run and why.
